services:
  # Moodle Backend Service
  - type: web
    name: lumina-moodle-backend
    env: docker
    region: oregon
    plan: free
    startTimeoutSeconds: 1200
    dockerfilePath: ./Dockerfile
    envVars:
      # Paste your connection strings into these in the Render Dashboard
      - key: DATABASE_URL
        value: postgresql://moodle_db_user:lPLEBqlshzn2LU27FXnPtR0y0ANV9Wsg@dpg-d78pfqh4tr6s73cdloqg-a/moodle_rgjo
      - key: MOODLE_DATA_DIR
        value: /var/moodledata
      - key: MOODLE_ADMIN_PASS
        value: Admin1234!
